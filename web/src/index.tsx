import React from 'react';
import ReactDOM from 'react-dom';
import { ConfiguredApp } from './views/App';

ReactDOM.render(
    <ConfiguredApp />,
    document.getElementById('root') as HTMLElement,
);
